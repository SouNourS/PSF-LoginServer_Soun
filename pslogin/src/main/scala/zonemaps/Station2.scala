package zonemaps

import net.psforever.objects.zones.{MapScale, ZoneMap}

object Station2 { // station2
  val ZoneMap = new ZoneMap("station2") {
    Scale = MapScale.Dim1024
    Checksum = 0L

  }
}
