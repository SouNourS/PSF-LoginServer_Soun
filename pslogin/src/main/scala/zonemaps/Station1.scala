package zonemaps

import net.psforever.objects.zones.{MapScale, ZoneMap}

object Station1 { // station1
  val ZoneMap = new ZoneMap("station1") {
    Scale = MapScale.Dim1024
    Checksum = 0L

  }
}
