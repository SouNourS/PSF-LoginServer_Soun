package zonemaps

import net.psforever.objects.zones.{MapScale, ZoneMap}

object Station3 { // station3
  val ZoneMap = new ZoneMap("station3") {
    Scale = MapScale.Dim1024
    Checksum = 0L

  }
}
